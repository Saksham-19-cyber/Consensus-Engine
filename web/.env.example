# Consensus Engine Frontend Environment Configuration
# Set this to your deployed FastAPI backend URL (e.g. on Render, Railway, or Fly.io)
# Note: The backend runs long-lived negotiation graphs and cannot run as Vercel serverless functions.
NEXT_PUBLIC_API_URL=http://localhost:8000
